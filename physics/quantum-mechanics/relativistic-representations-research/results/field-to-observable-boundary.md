# Field-to-observable boundary

Status: supported abstractly and on one regulated scalar model across two radial
form-factor families

Free representation data and local field realization construct admissible
one-particle content. An interacting Hamiltonian, preparation, and observable are
additional inputs. From them the departure map `B=QHJ` constructs the visible
measure `M_B=B^dagger E_Q B`, whose resolvent, Fourier, moments, and continuum
boundary feed several observables.

For the regulated scalar/two-level benchmark, the same field-derived radial measure
produces one bound/effective-mass coefficient and one finite-time emitted-boson
event. Operator/Feshbach and Duhamel bounds certify the ground and short-time
regimes. The shell/moment/action construction transfers unchanged from an analytic
Gaussian mass adapter to a numerical algebraic-tail adapter, with mass and tail
errors entering the same two certificates. This bounded transfer does not imply a
long-time decay rate, exact dressed measure, resonance, scattering matrix, or
general anisotropic/operator-valued channel.

For the scalar--spin-two threshold bench, an isolated Fock pole is not admitted.
Finite detector resolution nevertheless constructs nested spectral effects. On the
positive generated kernel, conditioning on a coarser accepted window produces a
minimal cyclic realization and the exact response

```text
mu=m^2,
F_mu(Delta|Lambda)=J_mu(Delta)/J_mu(Lambda),
J_mu(Delta)=2mu[Delta^2-mu Delta+mu^2 log(1+Delta/mu)].
```

This ratio is independent of overall cut normalization and local subtraction and
has small-window law `(Delta/Lambda)^2`. It is supported inside the minimal gauge-
fixed spectral realization, but it does not descend directly from the relatively
Ward-closed 1PI packet. A positive finite window evaluates the external ideal
generator `s-m^2` nontrivially; a positive functional annihilating that generator
must instead be supported at the shell, where this model has no pole atom.

The observable compiler therefore has a second repair obligation. After action
contacts cancel internal Ward descendants, an external grammar must cancel the
detector's value on the remaining inverse-kernel ideal. Only the repaired operation
is admitted as a gauge-invariant detector effect.

Thus the field equation is an intermediate realization contract. The predictive
compression begins only after dynamics, preparation, observable, and error regime
are explicit.

The resolved fermion-event branch now makes the same point at an infrared boundary.
The endpoint of the complete three-photon density factors exactly through the
unquotiented hard Compton effect:

```text
H=1/(128pi),
a=H [1/(8pi^2)] [lim E^2 abs(J dot epsilon)^2]
 =1/(1024pi^3).
```

One positive eikonal kernel consequently generates the leading virtual,
unresolved-real, and resolved-real detector-bin weights. Their interval identity
preserves the inclusive effect and removes the infrared regulator at leading
power. This is a reusable channel operation rather than three diagram-specific
counterterms.

The exact full density is nevertheless `a/E^2-a/E+O(1)` on both soft arms. The
leading tool therefore exposes an unmatched logarithm and refuses full infrared
completion. The result separates a genuine compression—common-kernel bin
transfer—from the still-open task of generating subleading and finite virtual
information.
